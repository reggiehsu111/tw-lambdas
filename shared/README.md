# shared/

Common utility modules shared across Lambda functions.

Files placed here are automatically bundled into every Lambda package.

## Examples

- `db.py` — RDS connection helpers
- `secrets.py` — AWS Secrets Manager wrapper
- `tw_utils.py` — Taiwan stock/futures utilities
